// src/config/slotConfig.js

// Keep these in sync with backend/slot_config.py
export const NUM_REELS = 5;
export const NUM_ROWS = 3;

// For now, just some placeholder symbols to show on the UI
export const STATIC_SYMBOLS = ["🍒", "🍋", "⭐", "🍇", "🔔"];

